export const API_BASE_URL = 'http://localhost:8000';

export const endpoints = {
    suggestAdmission: `${API_BASE_URL}/suggest-admission`,
    aiChat: `${API_BASE_URL}/ai-chat`,
    submitApplication: `${API_BASE_URL}/submit-application`, // Assuming this will be used later or is used elsewhere
};
